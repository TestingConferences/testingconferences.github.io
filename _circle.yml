## Customize test commands
machine:
  ruby:
    version: 2.1.2

dependencies:
  post:
    - bundle exec jekyll build

test:
  pre: # also try override
    - bundle exec jekyll serve
  post:
    - bundle exec rspec
